# 轻译实测记录

## V2.8.2：嵌入申请表单

2026-09-12：核对 `developers.google.com/search/apis/trends?hl=zh-cn#apply` 的公开页面内容后确认，主文档已经是中文，申请区域是来自 `docs.google.com` 的跨域 iframe；旧版因只登记顶层 frame 而漏掉整个表单。页面上的 `replay_5`、`calendar_month`、`call_merge` 等是 Material 图标名称，继续保持为图标。

本版会发现有实际显示尺寸的 iframe，在整页翻译按钮的直接用户操作中请求对应嵌入来源权限，并把每个 frame 按 `tabId + frameId + documentId` 独立登记、排队和回传。新增后台集成回归确认同一标签页的主文档与跨域 iframe 不互相替换任务，最终结果分别发送给正确的 documentId。73 项自动化测试与快速静态检查通过。Chrome 浏览器控制本轮连续两次无法加载请求策略，因此实际页面仍需重新加载扩展后复核。

---

## V2.8.1：Google Ads 界面覆盖

2026-09-12：在用户已打开的 `business.google.com` 和 Google Ads 关键词规划师页面读取可见结构后确认，漏译集中在全局排除的按钮、导航、侧栏、工具栏与页脚。源码已允许翻译普通文字按钮，并为 `ads.google.com`、`business.google.com` 启用完整可读界面范围；Material 字体图标、密码表单、输入控件及代码区域继续跳过。

外部本地 Chromium 使用 Google UI 结构夹具验证：`Keyword Planner`、`Performance Planner`、`Shared library`、`Saved keywords`、正文和 `Refresh data` 按钮进入请求并原位显示；`refresh` 图标、密码表单中的 `Sign in` 与 `<code>show()</code>` 未进入请求。翻译后点击按钮，原有事件计数从 0 变为 1，没有错误覆盖层。实际 Google 标签页当时仍运行重新加载前的 V2.8 内容脚本，因此不把旧页面状态记录为修复后的结果。

---

## V2.8：滚动预翻译

2026-09-12：72 项快速自动化测试通过。新增覆盖当前屏幕优先、下方两屏批量预取、滚动前不修改离屏 DOM、滚动后复用缓存且不新增请求、快速跳转中断无关 API 预取、预取失败延迟到可见后重试、动态代码区域失效，以及关闭预翻译后保持仅翻译可见内容。

外部本地 Chromium 模拟页验收通过：首屏完成后产生 1 个 `prefetch` 请求，批量包含第二、第三屏共 4 段；完成预取后页面记录 4 段已准备内容，第二屏仍保持原文。滚动到第二屏后 82 ms 显示缓存译文，请求总数保持 2，没有错误覆盖层。82 ms 是本地 DOM 调度耗时，不包含真实 DeepSeek 网络延迟，也不代表所有设备和网页的固定速度。本轮没有调用付费 API；实际安装在 Chrome 中仍需重新加载扩展后由用户验收。

---

## V2.7：技术句子保护与短窗口收集

2026-09-12：60 项快速自动化测试通过。新增覆盖整句占位符还原、遗漏/重复标记拒绝、不显示残缺标记、半段路径选择、动态路径变化、固定 80 ms 合批窗口、选区状态/取消及缓存查找。静态检查覆盖语法、Manifest、面板引用与安全输出规则。

真实 API 仅请求一次、3 句固定公开技术示例：9 个旧片段变为 3 个句子单元，首 token 2.033 秒、完成 2.504 秒，技术文本均正确本地还原。无同轮旧版或竞品速度对照，不发布整页提速倍数。完整记录为 reports/api-protected-sentences.json；竞品来源、实现范围和剩余问题见 reports/V2.7_COMPARISON.md。

外部 Chrome 本地检查调用遇到自动权限审批超时，未确认测试页打开，未完成真实扩展 UI 验收。临时预览服务已停止；没有新增翻译服务或修改浏览器管理页权限。

---

## V2.6：API 两路并行、流处理解耦与缓存容量

2026-09-12：48 项快速自动化测试通过，覆盖两路乱序流合并、用量汇总、取消、一路失败中止其他路且不重发、受阻显示回调不阻塞网络读取、150 片段第二轮缓存复用，以及后台路由和缓存别名恢复。快速静态检查覆盖 JavaScript、Manifest 和面板引用。

真实调用官方 API 共 8 个请求，使用公开 Python 文档 18 段快照。单请求两次完成 4.635 / 7.484 秒，两路对照 3.749 / 3.786 秒；最终模块复查首 token 8.078 秒、首段至少 20 字元 8.298 秒、整批 10.767 秒。慢样本保留，不能用小样本宣称稳定提速或追平竞品。报告不含 Key 或返回译文，原始记录为 reports/api-latency-diagnosis.json；详细方法、费用和官方竞品来源见 reports/V2.6_PERFORMANCE.md。

Chrome 浏览器工具先前连续两次无法加载请求策略，本次未绕过限制；未完成 V2.6 实际扩展 UI 验收，也没有竞品速度数据。当前测试均不代表点击至真实屏幕绘制的耗时。

---

## V2.5：首屏启动与中断恢复

2026-09-12：改为首屏优先、分段索引；减少离屏规则计算与主题切换重扫；动态节点失效不再停止整页；失败保留已完成译文并提供继续按钮；划选排队优先及等待期限协调；面板独立读取并行。

38 项自动化测试及快速静态检查通过。新增测试覆盖队列优先级/期限、索引遍历过程中当前节点被替换仍继续、全文索引完成前发起处理、分段执行及取消停止。索引调度测试使用受控节点与模拟时间，不是浏览器性能数据。

Chrome 自动化连续两次返回“Unable to load browser request-header policy”，未能进入本地测试页。本版新增的启动页、主题切换、动态正文及继续按钮尚未完成真实浏览器验收；未调用付费 API、未测竞品耗时，不报告虚构的提速倍数。后续人工入口为 tests/startup.html，前序版本记录仍是历史结果。

---

## V2.4.1：减少碎片请求与重复上下文

2026-09-12：按实际压缩后的输入大小合批（最多 32 个可见片段），重复上下文只发送一次。源页面仍只收集当前可见内容，输出 JSON 格式和流式渲染保持不变。

31 项自动化测试与快速静态检查通过。新增检查覆盖片段合批、上下文引用完整性、长正文预算及 IPC 大小边界。固定 24 片段/4 组上下文样本：旧分批 6 次，新分批 1 次，数据字符 13,373 → 3,635（约减少 73%）。未调用付费 API、未重新进行 Chrome 或竞品耗时测试；上述数字不代表实际速度提升倍数。

---

## V2.4：继承原样式、上下文与网站范围

2026-09-11：译文继承原文颜色、字体和文字样式；增加混排句上下文、按网站保存正文优先/全部范围、高置信度目标语言跳过，以及排队任务发送前再次命中缓存。

28 项自动化测试通过，快速静态检查通过。新增测试覆盖：两个页面排队的 Read more 只发送一次；不同上下文的同词缓存隔离；敏感上下文拒绝；简繁差异、中文混排、英文与日韩文识别。

外部 Chrome 本地模拟检查通过：标题译文继承原来的 rgb(123,45,67) 和斜体；正文译文继承 rgb(30,80,150)、19px 和 700 字重；已是简体中文的内容不发送；正文模式跳过侧栏，全部范围会发送侧栏。React/Acme+ 混排片段带完整句子上下文，代码和路径仍使用保留标记。恢复原文后替换节点数为 0。本轮无付费 API 调用，未实测真实模型的复杂句翻译质量或 DeepSeek 网页会话；实际扩展更新仍需重新加载并刷新网页。

---

## V2.3：可见索引、片段缓存与敏感内容保护

2026-09-11：新增初始正文索引、IntersectionObserver 可见筛选及 DOM 变化增量更新；片段缓存按内容、语言与模式复用，同批重复片段只提交一次，部分命中与流式回复合并。API 流式绘制去掉逐次存储等待。增加密码表单、常见代码编辑器、疑似凭据、乱码与技术术语过滤，并支持自定义保留术语。

- 快速检查通过；25 项自动化测试全部通过。新增测试覆盖敏感内容后台拦截、术语和代码过滤、重复片段去重、跨编号缓存、语言/模式隔离、缓存清理及畸形回复拒绝。
- 外部 Chrome 本地模拟页验证：保护区域没有译文节点，代码、测试密码、疑似密钥和乱码不进入模拟请求；混排正文绿色原位替换。新增正文正常识别，整页扫描计数仍为 1；恢复后译文节点为 0。
- 滚动模拟页：首屏请求 1 次；滚至第二屏后共 2 次；返回首屏仍为 2 次，第三屏未发送，整页扫描计数为 1。
- 面板本地预览：保留术语可以输入和保存，提示正确，布局检查通过。控制台观察到第三方 MetaMask 扩展连接报错，未观察到本项目脚本错误。

本次为必要的本地模拟与后台集成检查，没有重新调用付费 API，也没有与竞品做端到端速度实测。模拟回复由按钮手动完成，页面耗时字段不能当作真实速度数据。实际安装扩展需要重新加载并刷新网页；本轮没有验证 DeepSeek 网页真实会话。

---

## V2.2：原位替换与代码保护

2026-09-11：绿色译文改为原位替换正文或精确划选范围，不再追加到原文下方。发送前保护 API/文件路径、URL、HTTP 方法、常见代码表达式，跳过 code/pre/kbd/samp/var 元素；取消未完成请求或恢复原文时还原对应的原始文字。

Chrome 本地专项模拟验证通过：用户提供的 11 项路径导航（含拆分的 /prefs/ 与 where 链接）DOM 完全不变；GET /api/v1/me、HTTP GET方法、code 中的 show、pre 中的代码完全不变，均未进入请求。混排内容显示为“使用 GET /api/v1/me 查看你的账号。”及“调用 show 显示结果。”，链接和代码节点保留。译文均在原来的父元素内以绿色 inline 形式展示，无下方副本；恢复后原文恢复，替换节点数为 0。局部划选 hello world 只替换为“你好，世界”，Before 与 after. 保留。

新增保护规则测试包含用户的全部路径、HTTP 方法、文件路径、代码表达式与普通正文混排。未为本次修改重复调用付费 API；下方成本数据是历史版本的实测。

---

## V2.1：滚动翻译与绿色纯文字

2026-09-11 更新：页面翻译仅发送当前可见段落，使用小批次并从未完成的 JSON 回复中流式显示对应译文；滚动或加载新内容后继续，已完成段落不重复请求。译文使用绿色 `#15803d`，独立自定义元素重置旋转、缩放、竖排及网页全局样式，保持无背景、无边框、无操作按钮。

Chrome 本地专项模拟检查：首屏 1 次请求，第二屏和第三屏均未发送；回复仍为 streaming 时绿色译文已显示；首屏完成后进入 waiting-scroll；滚动到第二屏请求数变为 2；返回首屏保持 2 次；第三屏未触发；取消后滚动不再请求。样式测得绿色 rgb(21,128,61)、transform/rotate 为 none、writing-mode 为 horizontal-tb、背景透明。

17 项自动检查通过，新增部分 JSON、转义和 Unicode 流处理校验。用户尚未提供实际倒置网页的网址，当前针对可复现的全局旋转/竖排样式污染场景验证。下方 API 成本与耗时是此前 V2 的真实记录，V2.1 改用更小的按屏幕批次后，不能将旧费用直接视为新版完整页面账单。

---

## 2026-09-12 V2.9 竞品兜底范围

参考沉浸式翻译的“翻译网页/翻译整个页面”双入口，新增一次性“翻译全部可读内容”、右键菜单和 `Alt+W`。范围消息从面板传至每个已注入 frame；运行中的正文任务可直接升级范围并复用已完成结果。强制模式可覆盖导航、侧栏、页脚以及网页误用的 `translate="no"` / `.notranslate`，但不能覆盖代码、密码、输入框、敏感信息、乱码或字体图标保护。

自动回归 76 项通过，快速 Manifest、语法、权限和面板引用检查通过。测试页使用 Chrome 验证全部范围与按钮事件；实际安装扩展更新后仍需在 `chrome://extensions` 重新加载才能验收快捷键和真实页面。

记录时间：2026-09-10T15:59:20+00:00。

## 2026-09-11 显示样式调整

译文改为原文下方的纯文字，沿用原文字体和颜色；移除背景、边框、标签及操作按钮。Chrome 本地模拟整页检查 7/7 段成功：译文背景透明、边框 0、无附加子元素，完成后进度条隐藏，原文未改变。语法与文件检查通过。此检查不替代尚待授权的免 Key 真实速度测试。

## 已完成与待完成

- 开发完成：Key / 免 Key 模式、划选翻译、整页翻译、默认原文下方插入、取消 / 移除译文、用量导出。
- Chrome 本地模拟交互：划选译文在原段落下方；整页 7/7 段对应；输入框与可编辑文本未变化；移除后注入译文为 0。
- 自动检查：16 项测试通过；Manifest、语法、权限、输出检查通过。覆盖 API 分片与截断、错误脱敏、计费、段落对应、后台取消、Key 不回传、worker 重启不重发，以及原网页模式回归。
- 真实 API：速度测试 10/10 成功，完整文档正文测试 10/10 成功。
- **待完成**：安装中的 V2 扩展真实 UI 验收，以及免 Key 模式 10 次速度测试。需要用户重新加载扩展，并明确允许浏览器工具访问翻译专用 DeepSeek 会话。未将模拟结果计入真实请求，也未虚构免 Key 速度。

## 费用测试方法

使用 `tests/pages/01.html` 至 `10.html`：Python 官方教程的 10 篇完整正文快照，保留标题、正文、来源与 PSF 许可；不是整站抓取，不含代码块、导航和动态加载内容。页面长度见下表。

基准脚本直接调用扩展生产代码 `extension/lib/api.js` 和同一段落提示词、批次上限、JSON 对应校验；逐页串行、关闭本地缓存。此数据验证翻译传输及费用，不冒充安装扩展后的 10 页浏览器全链路结果。

型号：`deepseek-flash`，关闭深度思考，目标语言简体中文。使用 API 响应的真实 usage（输入、缓存命中 / 未命中、输出 token）。未读取账号余额或账单。

价格按 [DeepSeek 官方价格表](https://api-docs.deepseek.com/quick_start/pricing/) 于 2026-09-10 核对：峰时每百万 token 输入命中 $0.006、未命中 $0.30、输出 $1.20；谷时减半。峰时为周一至周五 UTC 01:00–04:00、06:00–10:00，其他为谷时。本次按每条请求开始时间估算，最终以官方账单为准。

| 页 | 正文字符* | 段数 | 输入 token | 输出 token | 完成秒数 | 预估美元 |
|---|---:|---:|---:|---:|---:|---:|
| [1. 1. Whetting Your Appetite](https://docs.python.org/3/tutorial/appetite.html) | 4437 | 18 | 1109 | 830 | 4.494 | $0.00066435 |
| [2. 2. Using the Python Interpreter](https://docs.python.org/3/tutorial/interpreter.html) | 5342 | 28 | 1416 | 1119 | 4.827 | $0.00088380 |
| [3. 10. Brief tour of the standard library](https://docs.python.org/3/tutorial/stdlib.html) | 5897 | 47 | 1578 | 1213 | 5.544 | $0.00096450 |
| [4. 11. Brief tour of the standard library — part II](https://docs.python.org/3/tutorial/stdlib2.html) | 7581 | 46 | 1811 | 1438 | 6.435 | $0.00113445 |
| [5. 12. Virtual Environments and Packages](https://docs.python.org/3/tutorial/venv.html) | 4949 | 32 | 1301 | 1008 | 4.584 | $0.00079995 |
| [6. 13. What Now?](https://docs.python.org/3/tutorial/whatnow.html) | 3063 | 19 | 850 | 635 | 3.052 | $0.00050850 |
| [7. 14. Interactive Input Editing and History Substitution](https://docs.python.org/3/tutorial/interactive.html) | 1994 | 8 | 511 | 374 | 2.089 | $0.00030105 |
| [8. 15. Floating-Point Arithmetic: Issues and Limitations](https://docs.python.org/3/tutorial/floatingpoint.html) | 9482 | 50 | 2629 | 2065 | 9.180 | $0.00163335 |
| [9. 16. Appendix](https://docs.python.org/3/tutorial/appendix.html) | 4702 | 25 | 1267 | 1008 | 4.266 | $0.00079485 |
| [10. 8. Errors and Exceptions](https://docs.python.org/3/tutorial/errors.html) | 12580 | 71 | 3161 | 2358 | 10.745 | $0.00188895 |

*包含测试页的来源说明。不同于含广告、图片和无限滚动的商业网页。

10 页总计 **$0.00957375**；平均每页 **$0.00095737**；最少 **$0.00030105**，最多 **$0.00188895**。平均完成时间 **5.522 秒**。网页更长或输出语言不同，成本会变化。

## 速度测试

同一组 10 段英文，见 `tests/speed.html`。API 测量从发起 HTTP 请求到完整流结束，首字时间为收到首个正文片段；不包括浏览器工具操作耗时。待补测的网页模式需要包含页面提交与 DOM 读取开销，并单独注明口径。

| 样本 | 英文字符 | API 首字秒数 | API 完成秒数 | 免 Key 完成秒数 |
|---|---:|---:|---:|---:|
| 1 | 43 | 0.333 | 0.481 | 待测 |
| 2 | 128 | 0.508 | 0.670 | 待测 |
| 3 | 125 | 0.656 | 0.837 | 待测 |
| 4 | 149 | 0.667 | 0.860 | 待测 |
| 5 | 156 | 0.543 | 0.724 | 待测 |
| 6 | 152 | 0.726 | 0.920 | 待测 |
| 7 | 151 | 0.300 | 0.596 | 待测 |
| 8 | 159 | 0.318 | 0.454 | 待测 |
| 9 | 181 | 0.523 | 0.702 | 待测 |
| 10 | 244 | 0.658 | 0.985 | 待测 |

API 10 次平均 **0.7229 秒**，中位数 **0.713 秒**；本组预估合计 **$0.00035220**。免 Key 的新一轮 10 次尚未完成，当前不能给出两者倍数比较。

## 可复查材料与清理

- `reports/api-speed.json`：10 次真实 API 请求的 token、时间、译文与费用。
- `reports/api-pages.json`：10 页各批次的真实 token、时间、费用和格式验证结果。
- `reports/benchmark-pages.json`、`reports/benchmark-inputs.json`：页面来源与可重现输入。
- `scripts/benchmark-api.mjs`、`scripts/benchmark-pages.mjs`：可重跑生产传输基准，从标准输入接收 Key。
- 用户提供的 Key 仅用于进程内存中的 API 测试，没有写入项目、报告或安装包。
- 原 V1 的真实网页端验收记录已保存为 `reports/V1_TEST_REPORT.md`，不能代替 V2 的本轮对比。
